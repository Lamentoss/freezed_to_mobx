name: freezed_to_mobx
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  # path: ^1.8.0
  analyzer: ^2.3.0
  build: ^2.1.0
  source_gen: ^1.2.1
  code_builder: ^4.1.0
  path: ^1.8.1
  dart_style: ^2.0.0
  freezed_to_mobx_annotations:
    git: https://github.com/Lamentoss/freezed_to_mobx_annotations.git

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
